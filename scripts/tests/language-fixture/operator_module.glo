// The loading file must learn this level before parsing its own expressions.
Combine :: typeclass (T: type) {
    '%%' :: (left: T, right: T) -> T #operator(left, 19);
}
ssize ::: instance Combine {
    '%%' :: (left: ssize, right: ssize) -> ssize { return left * 10 + right; }
}
